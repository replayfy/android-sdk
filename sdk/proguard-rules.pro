# ProGuard rules for the SDK's own release build (when consumers run
# minifyEnabled on a dependency, which is rare). Most rules live in
# consumer-rules.pro instead so they apply to the host app's build.
