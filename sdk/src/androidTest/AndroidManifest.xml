<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Host activity for the privacy-masking instrumented test. Lives
             only in the test APK; never shipped in the published AAR. -->
        <activity
            android:name="com.replayfy.android.MaskTestActivity"
            android:exported="true" />
    </application>
</manifest>
